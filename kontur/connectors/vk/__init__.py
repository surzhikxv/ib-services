"""Коннектор ВКонтакте: посты сообщества + метрики (охваты/реакции) в озеро.

Работает на ОДНОМ user-токене (`VK_USER_STATS_TOKEN`, scope wall+stats+offline):
community-токен сообщества не умеет `wall.get` ("method is unavailable with group
auth"), поэтому контент и статистику тянем токеном админа. VK — РФ-родной,
ходим напрямую (без релея).
"""
