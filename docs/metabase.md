# Дашборд Metabase — настройка

Логика дашбордов лежит в БД как SQL-вьюхи (`kontur/dashboard/views.py`), а карточки —
в каталогах `kontur/dashboard/catalog.py` и `kontur/dashboard/social_catalog.py`.
Поэтому собрать панели можно двумя путями:
автоматически (скрипт) или руками (по таблице ниже). Боевая БД — Postgres из
`docker-compose`; вьюхи создаются сами при `db init` и старте API.

## Путь 1. Автоматически (на VPS, когда поднят compose)

```bash
docker build --build-arg VCS_REF=local -t kontur-app:latest .
docker compose up -d
# 1) один раз пройти мастер Metabase на http://<сервер>:3000 — создать админа
# 2) вписать в .env: METABASE_USER / METABASE_PASSWORD (URL уже http://metabase:3000)
docker compose run --rm app python -m kontur.cli metabase provision
```

Скрипт залогинится, добавит наш Postgres как источник, создаст вопросы из каталогов
и соберёт единый дашборд «Контур роста — аналитика» в коллекции «Контур роста».
Внутри него находятся вкладки «Бизнес», «Соцсети», «Контент», «Площадки»,
«TikTok», «ИИ-отчёты» и «Данные».

Над карточками создаётся общий фильтр «Период» типа **Date → All options**.
По умолчанию выбраны последние 30 дней; доступны относительные периоды, календарный
диапазон, текущий месяц/квартал/год, а очистка фильтра показывает всю историю.
Для контента период означает дату публикации, для подписок и оплат — дату события,
для динамики каналов — дату снимка. Шесть карточек текущего состояния не фильтруются:
воронка, свежесть источников, текущие подписчики площадок, полнота и свежесть данных.

Этот дашборд автоматически назначается домашней страницей Metabase. Прежний
«Соцсети — аналитика» архивируется, поэтому после входа пользователь видит одну
точку входа вместо ленты последних отдельных вопросов.

Повторный запуск обновляет существующие карточки и перемещает их в эту коллекцию
(идемпотентно по имени). Ориентир по API — Metabase v0.48+.

Демонстрационные `Sample Database`, коллекция `Examples` и дашборд
`E-commerce Insights` к проекту не относятся. После первоначальной настройки их
можно отправить в архив через интерфейс Metabase; провижининг их не использует.

> Автонастройка — ускоритель. Если версия Metabase разойдётся с API, надёжный путь —
> ручная сборка ниже: SQL карточек тот же, он уже проверен на данных.

## Путь 2. Руками (10–15 минут)

1. **Источник данных:** Admin → Databases → Add → PostgreSQL. Host `postgres`, port `5432`,
   база/логин/пароль — из `.env`. Сохранить, дождаться синхронизации схемы.
2. **Карточки:** New → SQL query → выбрать нашу БД → вставить SQL из колонки ниже →
   Visualization выбрать по колонке «Тип» → Save.
3. **Дашборд:** New → Dashboard «Контур роста — аналитика» → создать семь вкладок
   из списка выше и распределить по ним карточки.
4. **Период:** добавить dashboard-level фильтр Date → All options, задать по умолчанию
   Previous 30 days и подключить его к переменной `period` всех SQL-карточек, где она есть.
5. **Главная:** Admin → Settings → General → Homepage → Dashboard → выбрать
   «Контур роста — аналитика».

### Карточки дашборда

| Карточка | Тип | Вьюха | Назначение |
|---|---|---|---|
| Подписчиков | `scalar` | `v_subscribers` | Новые подписчики выбранного периода |
| Платящих | `scalar` | `v_payments` | Уникальные покупатели выбранного периода |
| Конверсия, % | `scalar` | `v_subscribers` | Доля новых подписчиков периода, которые стали покупателями |
| Оплат | `scalar` | `v_payments` | Оплаты выбранного периода |
| Выручка, ₽ | `scalar` | `v_payments` | Выручка выбранного периода |
| Воронка по этапам | `funnel` | `v_funnel` | Уникальные пользователи, дошедшие до каждого этапа |
| Оплаты и выручка по тарифу | `bar` | `v_payments` | Базовый / Стандарт / Премиум |
| По источникам трафика | `row` | `v_payments` | UTM start-ссылки по каналам; входы без метки — `direct` |
| Новые подписчики по неделям | `line` | `v_subscribers` | В Metabase можно переключать гранулярность (день/неделя/месяц) |
| Оплаты и выручка по неделям | `line` | `v_payments` | Точное время оплаты добирается вебхуком Prodamus |
| Свежесть источников | `table` | `v_connector_freshness` | Последний успешный или ошибочный запуск каждого коннектора |

SQL каждой карточки — в двух каталогах выше (поле `metabase_sql`). Для большинства
карточек это `SELECT ... FROM <вьюха>`, его же можно вставить руками.

### Социальные разделы единого дашборда

Все 31 карточка разнесена по шести коротким тематическим вкладкам:

1. **Соцсети** — KPI, сводка площадок, вклад в просмотры и реакции, общая динамика.
2. **Контент** — форматы и лучшие публикации по просмотрам и вовлечённости.
3. **Площадки** — аудитория, краткая сводка TikTok, YouTube Shorts против видео,
   лучшие посты Telegram и VK.
4. **TikTok** — удержание, досмотры, источники трафика, возраст, пол, география и поиск.
5. **ИИ-отчёты** — полный последний разбор и архив последних 50 недельных и разовых
   отчётов; каждый отчёт занимает всю ширину карточки, текст переносится и сохраняет
   исходные абзацы.
6. **Данные** — заполненность полей и свежесть каждой загрузки.

Строки сетки начинаются заново внутри каждой вкладки, поэтому дашборд не превращается
в одну длинную ленту. На самих карточках используются короткие заголовки без повторения
«Соцсети» или названия активной вкладки.

Текущие итоги считаются из `content.metrics` — последнего lifetime-снимка публикации.
Это важно для YouTube: строки `content_metrics` там являются дневными значениями и
не должны подменять lifetime-счётчик. Исторические графики строятся отдельно по
`channel_metrics`. Telegram-реакции суммируются из массива типов реакций, ответы
нормализуются как комментарии, пересылки — как репосты.

## Откуда берётся выручка (₽)

Выручка считается как `COALESCE(оплата.сумма, тариф.цена, 0)`. Сначала используется
реальная сумма из вебхука Prodamus, а если её нет — цена тарифа. Чтобы цифры были
ненулевыми, достаточно одного из:
- проставить цены тарифов: `UPDATE tariffs SET price = <₽> WHERE key IN ('basic','standard','premium');`
- или включить приём сумм из вебхука Prodamus (каркас приёмника готов: `POST /webhooks/prodamus`).

## Как проверить живой дашборд

Не фиксируйте текущие счётчики в документации: они меняются после каждого входа в
бот и оплаты. Проверка после провижининга:

1. Открыть единый дашборд и убедиться, что 11 бизнес-карточек и 31 социальная карточка загрузились без ошибок.
2. Переключить «Период» между 7, 30 и 90 днями, затем очистить фильтр; связанные карточки должны пересчитаться без ошибок.
3. Сверить бизнес-KPI с выборками из `v_subscribers` и `v_payments` за тот же период.
4. Сверить этапы с `SELECT * FROM v_funnel ORDER BY ordering`.
5. Проверить, что в «Свежести источников» присутствуют Telegram, TikTok, VK и YouTube.
6. Проверить, что `direct` отображается как «(прямой вход)», а не отдельный технический код.
